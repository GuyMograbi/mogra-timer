{
  "name": "mogra-timer",
  "version": "0.0.0",
  "scripts": {
    "start": "lite-server",
    "lint" : "standard",
    "clean" : "rm -rf dist",
    "build" : "mkdir -p dist/node_modules && cp index.html dist/index.html && cp -R app dist && cp -R node_modules/angular dist/node_modules/angular && cp -R node_modules/lodash dist/node_modules",
    "lint:fix" : "npm run lint -- --fix"
  },
  "devDependencies": {
    "babel": "^6.5.2",
    "lite-server": "^2.2.2",
    "standard": "^8.6.0"
  },
  "dependencies": {
    "angular": "^1.6.1",
    "lodash": "^4.17.4"
    
  }
}
